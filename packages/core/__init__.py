# core package – domain, application, infrastructure layers placeholder

# Domain entities, value objects, enums would live under `domain/`.
# Application services (use‑cases) under `application/`.
# Infrastructure (DB, blob store, messaging) under `infrastructure/`.
