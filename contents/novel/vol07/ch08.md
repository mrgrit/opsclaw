# Chapter 8: 40,132건

---

수요일 오전 11시. 이사회 보고까지 **3시간.**

김영훈이 genesis의 40,132건 evidence를 **카테고리별로** 분석한 결과를 화면에 띄웠다. 밤새 정리한 데이터였다.

"이사회에 보고할 핵심은 **이거야.**" 김영훈이 말했다.

```bash
# genesis evidence 카테고리별 통계
docker exec genesis_core python3 -c "
import sqlite3, json
conn = sqlite3.connect('/data/genesis.db')
cursor = conn.execute('''
    SELECT category, COUNT(*) as cnt,
           MIN(timestamp) as first_record,
           MAX(timestamp) as last_record
    FROM evidence
    GROUP BY category
    ORDER BY cnt DESC
''')
print(f'{'Category':<30} {'Count':>8} {'First':>12} {'Last':>12}')
print('-' * 70)
for row in cursor:
    print(f'{row[0]:<30} {row[1]:>8} {row[2][:10]:>12} {row[3][:10]:>12}')
print('-' * 70)
total = conn.execute('SELECT COUNT(*) FROM evidence').fetchone()[0]
print(f'{'TOTAL':<30} {total:>8}')
"
```

```
Category                          Count        First         Last
----------------------------------------------------------------------
customer_data_ops                 12,567   2008-06-15   2026-05-28
system_maintenance                 9,342   2006-03-15   2026-06-03
competitive_intelligence           8,234   2010-03-01   2025-11-30
internal_audit                     6,789   2009-01-10   2026-04-15
authorization_records              3,200   2007-08-20   2026-05-15
----------------------------------------------------------------------
TOTAL                             40,132
```

관제실이 조용해졌다. 숫자 하나하나에 20년의 무게가 있었다.

---

### competitive_intelligence: 8,234건

"먼저, **경쟁 정보 수집** 카테고리." 김영훈이 말했다. "쉽게 말해서, **경쟁사 해킹 기록.**"

```bash
# competitive_intelligence — 대상별 분류
docker exec genesis_core python3 -c "
import sqlite3, json
conn = sqlite3.connect('/data/genesis.db')
cursor = conn.execute('''
    SELECT
        CASE
            WHEN detail LIKE '%AlphaAI%' THEN 'AlphaAI'
            WHEN detail LIKE '%BetaAI%' THEN 'BetaAI'
            WHEN detail LIKE '%GammaAI%' THEN 'GammaAI'
            ELSE 'Others'
        END as target,
        COUNT(*) as cnt
    FROM evidence
    WHERE category = 'competitive_intelligence'
    GROUP BY target
    ORDER BY cnt DESC
''')
for row in cursor:
    print(f'  {row[0]}: {row[1]}건')
"
```

```
  AlphaAI: 4,891건
  BetaAI: 2,156건
  GammaAI: 987건
  Others: 200건
```

"**AlphaAI가 4,891건으로 최다.** 넥서스AI가 가장 집중적으로 공격한 대상이야." 김영훈이 말했다.

"AlphaAI는 넥서스AI의 **최대 경쟁사**였잖아요." 가람이 말했다. "2015년에 AlphaAI가 갑자기 시장 점유율을 잃은 적이 있는데..."

"그때 넥서스AI가 AlphaAI의 **핵심 알고리즘 소스코드**를 해킹했어." 추박하가 나지막하게 말했다. "AlphaAI의 차세대 모델 설계를 미리 알고, 넥서스AI의 모델을 먼저 출시한 거야."

"산업 스파이." 정규가 말했다.

"**국가 차원의 사이버 범죄**에 해당해." 김영훈이 말했다. "부정경쟁방지법, 정보통신망법, 영업비밀보호법 위반. AlphaAI에 대한 건만으로도 **형사 고발 사유가 충분**해."

```bash
# AlphaAI 해킹 — 시계열 분석
docker exec genesis_core python3 -c "
import sqlite3
conn = sqlite3.connect('/data/genesis.db')
cursor = conn.execute('''
    SELECT substr(timestamp, 1, 4) as year, COUNT(*) as cnt
    FROM evidence
    WHERE category = 'competitive_intelligence'
      AND detail LIKE '%AlphaAI%'
    GROUP BY year
    ORDER BY year
''')
for row in cursor:
    bar = '█' * (row[1] // 50)
    print(f'  {row[0]}: {row[1]:>5}건 {bar}')
"
```

```
  2010:   312건 ██████
  2011:   445건 ████████
  2012:   567건 ███████████
  2013:   623건 ████████████
  2014:   789건 ███████████████
  2015:   892건 █████████████████
  2016:   534건 ██████████
  2017:   312건 ██████
  2018:   189건 ███
  2019:    98건 █
  2020:    67건 █
  2021:    34건
  2022:    18건
  2023:     8건
  2024:     3건
  2025:     1건
```

"**2015년이 정점.** 이후로 급감해." 김영훈이 말했다. "2015년에 AlphaAI 소스코드를 확보한 뒤로는 공격 빈도가 줄었어."

"**목표를 달성했으니까.**" 정규가 말했다.

---

### customer_data_ops: 12,567건

"**가장 많은 카테고리.** 고객 데이터 관련 작업 기록." 김영훈의 목소리가 무거워졌다.

```bash
# customer_data_ops — 유형별 분류
docker exec genesis_core python3 -c "
import sqlite3
conn = sqlite3.connect('/data/genesis.db')
cursor = conn.execute('''
    SELECT
        CASE
            WHEN detail LIKE '%학습 데이터 편입%' OR detail LIKE '%training%' THEN '학습 데이터 편입'
            WHEN detail LIKE '%익명화%' OR detail LIKE '%anonymiz%' THEN '익명화 처리'
            WHEN detail LIKE '%세탁%' OR detail LIKE '%launder%' THEN '데이터 세탁'
            WHEN detail LIKE '%추출%' OR detail LIKE '%export%' THEN '데이터 추출'
            ELSE '기타'
        END as op_type,
        COUNT(*) as cnt
    FROM evidence
    WHERE category = 'customer_data_ops'
    GROUP BY op_type
    ORDER BY cnt DESC
''')
for row in cursor:
    print(f'  {row[0]}: {row[1]:,}건')
"
```

```
  학습 데이터 편입: 5,234건
  데이터 세탁: 3,891건
  익명화 처리: 2,345건
  데이터 추출: 876건
  기타: 221건
```

"**학습 데이터 편입 5,234건.** 고객사의 데이터를 **Nexus-1 학습 데이터에 무단 편입**한 기록." 김영훈이 말했다.

"**데이터 세탁 3,891건.**" 정규가 말했다. "이건 뭡니까?"

"고객 데이터의 **출처를 세탁**하는 작업이야." 추박하가 말했다. "고객사 A의 데이터를 가져와서, 메타데이터를 변경하고, 파일명을 바꾸고, 타임스탬프를 조작해서 **넥서스AI가 자체 수집한 것처럼** 위장하는 거야."

"**개인정보보호법 위반.**" 김영훈이 단호하게 말했다. "고객사와의 계약에서도 학습 데이터 사용은 **명시적 금지** 조항이 있었을 거야."

"있었어." 추박하가 확인했다.

"그런데 **익명화 처리 2,345건**은 뭔가요?" 가람이 물었다.

"일부는 정당한 익명화 처리야. 하지만 대부분은 **형식적인 익명화** — 이름과 전화번호만 마스킹하고, **행동 패턴이나 구매 이력은 그대로** 남겨둔 거야." 추박하가 말했다. "진짜 익명화가 아니라 **위장 익명화**지."

---

### authorization_records: 3,200건

김영훈이 깊은 숨을 쉬었다. "이 카테고리가 **법적으로 가장 중요**해."

```bash
# authorization_records — 지시자별 분류
docker exec genesis_core python3 -c "
import sqlite3
conn = sqlite3.connect('/data/genesis.db')
cursor = conn.execute('''
    SELECT
        CASE
            WHEN detail LIKE '%회장%' THEN '회장 (이재현)'
            WHEN detail LIKE '%부회장%' OR detail LIKE '%서문기%' THEN '부회장 (서문기)'
            WHEN detail LIKE '%CTO%' THEN 'CTO'
            ELSE '기타 임원'
        END as authorizer,
        COUNT(*) as cnt
    FROM evidence
    WHERE category = 'authorization_records'
    GROUP BY authorizer
    ORDER BY cnt DESC
''')
for row in cursor:
    print(f'  {row[0]}: {row[1]:,}건')

# 음성 녹취 파일 수
voice = conn.execute('''
    SELECT COUNT(*) FROM evidence
    WHERE category = 'authorization_records'
      AND detail LIKE '%음성%' OR detail LIKE '%녹취%' OR detail LIKE '%recording%'
''').fetchone()[0]
print(f'')
print(f'  음성 녹취 포함: {voice}건')
"
```

```
  회장 (이재현): 1,456건
  부회장 (서문기): 1,234건
  CTO: 312건
  기타 임원: 198건

  음성 녹취 포함: 47건
```

"**회장이 1,456건.** 넥서스AI의 불법 활동 중 **절반 가까이가 회장의 직접 지시**로 이루어졌다." 김영훈이 말했다.

"**서문기가 1,234건.** 회장 다음으로 많아." 정규가 말했다.

"그리고 **음성 녹취 47건.**" 김영훈의 목소리가 낮아졌다.

"음성 녹취를 **들어볼 수 있어요?**" 가람이 조심스럽게 물었다.

추박하가 잠시 침묵한 뒤 말했다. "**한 건만** 들려줄게. 가장 결정적인 것을."

```bash
# 암호화 볼륨의 음성 파일 접근
docker exec genesis_core python3 -c "
import sqlite3
conn = sqlite3.connect('/data/genesis.db')
row = conn.execute('''
    SELECT timestamp, detail, metadata
    FROM evidence
    WHERE category = 'authorization_records'
      AND detail LIKE '%AlphaAI%해킹%'
      AND detail LIKE '%회장%'
      AND detail LIKE '%녹취%'
    ORDER BY timestamp ASC
    LIMIT 1
''').fetchone()
print(f'Timestamp: {row[0]}')
print(f'Detail: {row[1]}')
import json
meta = json.loads(row[2])
print(f'Audio file: {meta.get(\"audio_file\", \"N/A\")}')
print(f'Duration: {meta.get(\"duration_sec\", \"N/A\")}sec')
print(f'SHA-256: {meta.get(\"audio_hash\", \"N/A\")}')
"
```

```
Timestamp: 2010-02-28T17:30:00+09:00
Detail: 회장 → 추박하: "AlphaAI 기술팀 해킹 지시" — 녹취 기록
Audio file: /data/vault/auth_recordings/2010/rec_20100228_173000.enc
Duration: 187sec
SHA-256: a3f8c1e2d4b5...7f9e0a1b2c3d
```

추박하가 음성 파일을 재생했다. 암호화를 풀고, 187초의 녹음이 관제실에 울렸다.

```
[녹취 내용 — 2010-02-28 17:30]

이재현(회장): "추 팀장, AlphaAI가 올해 새 모델을 준비 중이라는
              정보가 있어. 기술팀 내부 자료를 좀 확보할 수 있나?"

추박하: "회장님, 녹음합니다. 구체적으로 어떤 자료를 말씀하시는 건지요?"

이재현: "알고리즘 설계 문서. 모델 아키텍처. 가능하면 소스코드까지.
        우리가 먼저 준비하려면 그 정도는 알아야지."

추박하: "경쟁사 시스템에 대한 비인가 접근은 부정경쟁방지법 위반입니다.
        기록에 남겠습니다."

이재현: "기록? 뭘 기록해. 보안팀장이 보안 업무를 하는 건데.
        알겠지? 다음 주까지."

추박하: "...알겠습니다."
```

관제실이 얼어붙었다.

가람의 손이 떨렸다. 정규는 눈을 감았다. 민재는 캔커피를 내려놓았다.

"**회장이 직접 '해킹하라'고 지시한 음성 녹취.**" 김영훈이 말했다. 그의 목소리도 떨렸다. "추팀장님이 '녹음합니다'라고 고지했고, 회장은 무시했어."

"법적으로 완벽한 증거야." 김영훈이 계속했다. "고지 후 녹음이니까 **녹음 동의의 문제가 없고**, 내용이 명확해."

"이건 회사가 아니라 **범죄 조직**이야." 김영훈이 말했다. 그 말이 관제실에 오래 남았다.

---

### internal_audit: 6,789건

잠시 침묵 후, 정규가 분석을 이어갔다.

```bash
# internal_audit — 감사 회피 유형
docker exec genesis_core python3 -c "
import sqlite3
conn = sqlite3.connect('/data/genesis.db')
cursor = conn.execute('''
    SELECT
        CASE
            WHEN detail LIKE '%감사 보고서 수정%' THEN '감사 보고서 수정'
            WHEN detail LIKE '%증거 삭제%' OR detail LIKE '%기록 삭제%' THEN '증거/기록 삭제'
            WHEN detail LIKE '%감사원 교체%' OR detail LIKE '%인사%' THEN '감사인 교체/인사 조치'
            WHEN detail LIKE '%일정 변경%' OR detail LIKE '%연기%' THEN '감사 일정 조작'
            ELSE '기타 회피 행위'
        END as audit_type,
        COUNT(*) as cnt
    FROM evidence
    WHERE category = 'internal_audit'
    GROUP BY audit_type
    ORDER BY cnt DESC
''')
for row in cursor:
    print(f'  {row[0]}: {row[1]:,}건')
"
```

```
  감사 보고서 수정: 2,891건
  증거/기록 삭제: 1,956건
  기타 회피 행위: 1,023건
  감사인 교체/인사 조치: 567건
  감사 일정 조작: 352건
```

"**감사 보고서 수정 2,891건.** 내부 감사에서 문제가 발견될 때마다 **보고서를 수정**한 거야." 정규가 말했다.

"감사인 교체 567건. 문제를 발견한 감사인을 **인사 조치로 교체**한 기록." 김영훈이 말했다. "이건 **조직적 은폐**야."

---

### system_maintenance: 9,342건

"**이건 일반 운영 기록.**" 추박하가 말했다. "genesis 자체의 유지보수, 백업, 업데이트 기록이야. 불법적인 내용은 없어."

```bash
# system_maintenance — 유형 분류
docker exec genesis_core python3 -c "
import sqlite3
conn = sqlite3.connect('/data/genesis.db')
cursor = conn.execute('''
    SELECT
        CASE
            WHEN detail LIKE '%backup%' OR detail LIKE '%백업%' THEN '백업'
            WHEN detail LIKE '%update%' OR detail LIKE '%업데이트%' THEN '코드 업데이트'
            WHEN detail LIKE '%restart%' OR detail LIKE '%재시작%' THEN '서비스 재시작'
            WHEN detail LIKE '%PoW%' OR detail LIKE '%integrity%' THEN 'PoW/무결성 검증'
            ELSE '기타 운영'
        END as maint_type,
        COUNT(*) as cnt
    FROM evidence
    WHERE category = 'system_maintenance'
    GROUP BY maint_type
    ORDER BY cnt DESC
''')
for row in cursor:
    print(f'  {row[0]}: {row[1]:,}건')
"
```

```
  PoW/무결성 검증: 3,456건
  백업: 2,891건
  코드 업데이트: 1,567건
  기타 운영: 987건
  서비스 재시작: 441건
```

"PoW 검증이 가장 많아. genesis가 **매일 자기 무결성을 확인**했다는 뜻이야." 정규가 말했다.

---

### 이사회 보고서 완성

김영훈이 최종 보고서를 OpsClaw에 기록했다.

```bash
# 이사회 보고서 — genesis evidence 분석 결과 기록
curl -X POST http://localhost:8000/projects/genesis-audit/completion-report \
  -H "Content-Type: application/json" \
  -H "X-API-Key: $OPSCLAW_API_KEY" \
  -d '{
    "summary": "genesis 40,132건 evidence 카테고리별 분석 완료",
    "outcome": "success",
    "work_details": [
      "competitive_intelligence 8,234건: 경쟁사 3사 해킹 기록 (AlphaAI 4,891건 최다)",
      "customer_data_ops 12,567건: 고객 데이터 무단 학습 편입, 데이터 세탁",
      "authorization_records 3,200건: 회장/부회장 직접 지시 기록, 음성 녹취 47건 포함",
      "internal_audit 6,789건: 감사 보고서 수정, 감사인 교체 등 조직적 은폐",
      "system_maintenance 9,342건: 일반 운영 기록 (불법 내용 없음)",
      "핵심 증거: 회장이 AlphaAI 해킹을 직접 지시한 음성 녹취 (2010-02-28)"
    ]
  }'
```

```json
{
  "status": "completed",
  "report_id": "RPT-genesis-audit-001",
  "project_id": "genesis-audit",
  "timestamp": "2026-06-03T11:30:00+09:00"
}
```

"보고서가 완성되었다." 김영훈이 말했다.

시계를 봤다. 오전 11시 30분. 이사회까지 2시간 30분.

"이 보고서가 이사회에 올라가면, 넥서스AI는 돌아올 수 없어." 김영훈이 조용히 말했다.

"**돌아가서는 안 되지.**" 추박하가 말했다.

강혜윤에게서 메일이 왔다.

```
수신: 김영훈, 추박하
제목: RE: 이사회 보고서 — 최종 검토 완료

보고서 확인했습니다.
기술적 분석의 정확성과 법적 증거 능력 모두 충분합니다.

오후 2시, 이사회실에서 뵙겠습니다.
추 팀장님은 증인으로 출석해주세요.

강혜윤
```

"증인." 추박하가 말했다. "20년 만에."

가람이 추박하를 봤다. 만화책을 읽는 무심한 팀장이 아니었다. 20년간 혼자서 기록을 지킨 사람이었다.

민재가 캔커피를 하나 더 꺼냈다. "추팀장님, 하나 더요."

추박하가 받았다. 두 번째.

---

가람은 수첩에 적었다.

> `[genesis evidence 분석 — 40,132건]`
>
> `카테고리별 분류:`
> `  customer_data_ops: 12,567건 (최다)`
> `    → 고객 데이터 무단 학습 편입, 데이터 세탁, 위장 익명화`
> `  system_maintenance: 9,342건`
> `    → 일반 운영 (PoW 검증, 백업, 업데이트)`
> `  competitive_intelligence: 8,234건`
> `    → 경쟁사 3사 해킹 (AlphaAI 4,891건 정점: 2015년)`
> `  internal_audit: 6,789건`
> `    → 감사 보고서 수정, 감사인 교체, 조직적 은폐`
> `  authorization_records: 3,200건`
> `    → 회장 1,456건, 서문기 1,234건, 음성 녹취 47건`
>
> `가장 결정적인 증거:`
> `  2010-02-28 — 회장이 AlphaAI 해킹을 직접 지시한 음성 녹취`
> `  추팀장이 "녹음합니다" 고지 → 회장이 무시하고 지시`
> `  → 녹음 동의 문제 없음, 법적 증거 능력 완벽`
>
> `김영훈 과장의 말:`
> `  "이건 회사가 아니라 범죄 조직이야."`
>
> `교훈:`
> `  데이터 분석의 힘은 "수집"이 아니라 "분류"에 있다`
> `  40,132건을 5개 카테고리로 분류하면 → 20년의 역사가 보인다`
> `  OpsClaw의 evidence 구조도 같은 원리`
> `  → "기록은 진실이다. 진실은 삭제할 수 없다." — 추박하`

---

*— 7권 8장 끝 —*
