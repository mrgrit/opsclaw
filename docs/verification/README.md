# OpsClaw Verification Channel

이 디렉터리는 작업별 검수 결과와 실행/테스트 요약을 저장하는 검수 채널이다.

규칙:
- 작업마다 `WORK-번호.md` 파일을 생성한다.
- 각 파일에는 브랜치, 커밋, 수정 파일, 실행 명령, 테스트 명령, 핵심 결과, 미해결 사항을 기록한다.
- 긴 터미널 출력 전체를 채팅에 붙이지 않고, 이 디렉터리에 정리한 뒤 push한다.
